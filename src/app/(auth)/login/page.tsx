import { PlaceholderPage } from "@/components/common/PlaceholderPage";

export default function LoginPage() {
  return (
    <PlaceholderPage
      eyebrow="Authentication"
      title="Login"
      description="Sign-in form and authentication handoff will live here."
    />
  );
}
